@page
@model BoardGameDB.Pages_PlayStyles.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>PlayStyle</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.PlayStyle.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.PlayStyle.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.PlayStyle?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
